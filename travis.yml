language: php

php:
  - 5.3
  - 5.4
  - 5.5
  - 5.6
  - 7.0
  - 7.1
  - hhvm

before_script:
  - cp .env.travis .env
  - composer self-update
  - composer install --no-interaction --prefer-source --dev
  - php artisan key:generate

script:
  - vendor/bin/phpunit